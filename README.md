# geosite-geoip-filters-v2raya-openwrt

1. use `routing-config.txt` in RoutingA config inside v2raya web gui
2. put both files `geoip.ru` and `geosite.ru` into directory inside your openwrt `/usr/share/xray/`
3. profit!
